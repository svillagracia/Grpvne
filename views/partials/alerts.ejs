<% if(typeof alerts !== 'undefined'){ %>
    <% for(key in alerts){ %>
        <% alerts[key].forEach(function(thisMsg){ %>
            <div class="col-md-4 col-md-offset-4 alert alert-<%= key %>"><%= thisMsg %></div>
        <% }); %>
    <% } %>
<% } %>